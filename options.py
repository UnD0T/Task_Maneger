from aiogram.types import BotCommand

private = [
    BotCommand(command='keyboard_remove', description='removes keyboard'),
    BotCommand(command='about', description='info about bot'),
    BotCommand(command='links', description='manage links'),
    BotCommand(command='tasks', description='manage tasks'),
    BotCommand(command='schedule', description='your schedule'),

]
